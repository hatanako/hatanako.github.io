---
title: 从零开始做类银练习：一
date: 2026-03-09
categories: 个人练习
tags:
  - 类银
---

感觉自己实在是理论看太多了实践做的太少，所以有了这样一个项目，后续应该会是想到什么做什么吧，也许大概。

---
<br>

总之先构建一个场景，~~具体怎么构建的是个人都会吧~~，然后在场景上放置一个方块，为它挂上`Collider`；再放置一个角色(我个人一般倾向于把角色分为两个部分，父对象作为挂载碰撞检测和脚本的对象，子对象作为动画和贴图的对象)，为它挂上`Collider`和`Rigidbody`<br>
这样一个基础的场景就搭好了，就像这样：


{% asset_img 1.png %}

好了，现在我们可以为角色挂载上动画，我个人的习惯是在项目中新建一个`ArtRes/Anim/Animator`的目录用来存放`Animator Controller`，然后在`Anim`下创建一个`Animation`用来存放动画文件，就像这样：

{% asset_img 2.png %}

好的，现在我们的基础工作都做完了，接下来就是**创建脚本**和**设置动画**了，首先我们先来到动画界面，将我们创建好的`Idle`和`Move`动作连接起来，并创建一个变量：`isMove`，用来检测玩家是否移动，从而切换状态，就像这样：

{% asset_img 3.png %}

接下来我们就可以开始写脚本了，在`Assets/Scripts/Player`下创建一个脚本，叫做`PlayerController`，我们需要把这个类挂载到`Player`对象上，然后编写以下代码：
``` csharp
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    private Rigidbody2D rb;
    private Animator anim;

    public float moveSpeed = 5f;

    void Start()
    {
        rb = GetComponent<Rigidbody2D>();
        anim = GetComponentInChildren<Animator>();
    }

    void FixedUpdate()
    {
        // 使用 GetAxisRaw 获取离散输入（瞬间为 -1,0,1，无平滑）
        float horizontal = Input.GetAxisRaw("Horizontal");

        // 直接设置速度，保留 y 轴速度（用于重力、跳跃等）
        rb.velocity = new Vector2(horizontal * moveSpeed, rb.velocity.y);

        // 根据实际速度（而非输入）更新动画，避免输入与移动不同步
        bool isMoving = Mathf.Abs(rb.velocity.x) > 0.01f;
        anim.SetBool("isMove", isMoving);
    }
}
```
这样就实现了一个角色的基本移动了，很简单对吧，~~但是我太久没自己写了居然犯了一大堆低级错误~~，然后现在我们可以发现，玩家在移动的时候没有进行翻转，往左走的时候居然还是面向右边的，这时候我们就需要再添加代码了：
``` csharp
//PlayerController.cs
private void Update()
{
    if(Input.GetAxisRaw("Horizontal") > 0)
    {
        transform.localScale = new Vector3(1, 1, 1);
    }
    else if(Input.GetAxisRaw("Horizontal") < 0)
    {
        transform.localScale = new Vector3(-1, 1, 1);
    }
}
```
这样就实现了翻转，很简单对不对，但是可以看到我们的代码结构已经开始逐渐混乱起来了，我们需要稍微整理一下代码：
``` csharp
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    private Rigidbody2D rb;
    private Animator anim;

    public float moveSpeed = 5f;

    void Start()
    {
        rb = GetComponent<Rigidbody2D>();
        anim = GetComponentInChildren<Animator>();
    }

    void FixedUpdate()
    {
        // 使用 GetAxisRaw 获取离散输入（瞬间为 -1,0,1，无平滑）
        float inputArrow = Input.GetAxisRaw("Horizontal");
        Move(inputArrow);
    }


    private void Update()
    {
        Flip();
    }

    /// <summary>
    /// 反转角色朝向，根据水平输入调整角色的 localScale.x 来实现翻转效果。
    /// </summary>
    private void Flip()
    {
        if (Input.GetAxisRaw("Horizontal") > 0)
        {
            transform.localScale = new Vector3(1, 1, 1);
        }
        else if (Input.GetAxisRaw("Horizontal") < 0)
        {
            transform.localScale = new Vector3(-1, 1, 1);
        }
    }

    /// <summary>
    /// 玩家移动方法，直接设置 Rigidbody2D 的 velocity 来实现移动。
    /// 根据输入方向调整水平速度，同时保留垂直速度（如重力和跳跃）。
    /// 动画状态根据实际速度更新，确保动画与移动同步。
    /// </summary>
    /// <param name="dir">输入的方向</param>
    private void Move(float dir)
    {
        // 直接设置速度，保留 y 轴速度（用于重力、跳跃等）
        rb.velocity = new Vector2(dir * moveSpeed, rb.velocity.y);

        // 根据实际速度（而非输入）更新动画，避免输入与移动不同步
        bool isMoving = Mathf.Abs(rb.velocity.x) > 0.01f;
        anim.SetBool("isMove", isMoving);
    }
}
```

这样就干净很多了不是吗，我们完全可以把下面的`Flip()`和`Move()`折叠起来，以后可能都不需要更改了，只需要看上面，噢，这里根据方向移动，简单清晰  

接下来我想做的是跳跃，作为一个横板游戏怎么可以没有跳跃呢？我们在下面创建一个方法来实现跳跃逻辑：
``` csharp
//PlayerController.cs


public float jumpForce = 5f;

private void Update()
{
    CheckGround();
    Flip();
    Jump();

}

/// <summary>
/// 跳跃方法
/// </summary>
private void Jump()
{
    if(Input.GetKeyDown(KeyCode.Space))
    {
        rb.velocity = new Vector2(rb.velocity.x, jumpForce);
    }
}
```

非常的简单对吧，嗯但是正常来说玩家是不可以一直跳跃的，那么有没有什么好的办法实现呢？这里我选择使用**射线检测**来做这么一个限制跳跃的功能，通过向下发射一条射线来检测玩家是否位于地面。  
首先我们需要添加一个`Ground`层级，提供给射线检测判断是否存在于地面，我们可以用`Raycast`来做，但是在这之前，如果这条射线不可见的话，调试起来就非常麻烦了，所以我添加了以下代码：
``` csharp
private bool isGrounded = true;
public float groundCheckDistance = 0.1f;
public Transform groundCheck;

private void OnDrawGizmosSelected()
{
    // 仅在物体被选中时绘制
    Gizmos.color = isGrounded ? Color.green : Color.red;
    Gizmos.DrawLine(groundCheck.position, groundCheck.position + Vector3.down * groundCheckDistance);
}
```
通过这么几行代码我们就可以直观的看到射线啦，同时我加了颜色，这样就可以清晰的判断玩家是否到达地面了。我们可以通过在`Player`下新建一个空物体，然后把它赋值给`groundCheck`，一条可以随意拖动的线就做好啦。( 还记得我上面说我个人习惯创建一个父物体一个子物体吗，就是干这个用的（）)


接下来就是检测碰撞，这里就不用我多说了吧，直接上代码了：
``` csharp
/// <summary>
/// 检测地面
/// </summary>
private void CheckGround()
{
    RaycastHit2D hit = Physics2D.Raycast(groundCheck.position, Vector2.down, groundCheckDistance, 1 << LayerMask.NameToLayer("Ground"));
    isGrounded = hit.collider != null;
    anim.SetBool("isGround", isGrounded);
}
```
好了，现在已经基本完成了！玩家的基本移动都可以做到了，现在我们来看看代码。。。。
``` csharp
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    private Rigidbody2D rb;
    private Animator anim;

    public float moveSpeed = 5f;
    public float jumpForce = 5f;

    private bool isGrounded = true;
    public float groundCheckDistance = 0.1f;
    public Transform groundCheck;

    void Start()
    {
        rb = GetComponent<Rigidbody2D>();
        anim = GetComponentInChildren<Animator>();
    }

    void FixedUpdate()
    {
        // 使用 GetAxisRaw 获取离散输入（瞬间为 -1,0,1，无平滑）
        float inputArrow = Input.GetAxisRaw("Horizontal");
        Move(inputArrow);
    }


    private void Update()
    {
        CheckGround();
        Flip();
        Jump();
        CheckJumpState();

    }

    /// <summary>
    /// 检测地面
    /// </summary>
    private void CheckGround()
    {
        RaycastHit2D hit = Physics2D.Raycast(groundCheck.position, Vector2.down, groundCheckDistance, 1 << LayerMask.NameToLayer("Ground"));
        isGrounded = hit.collider != null;
        anim.SetBool("isGround", isGrounded);
    }

    private void OnDrawGizmosSelected()
    {
        // 仅在物体被选中时绘制
        Gizmos.color = isGrounded ? Color.green : Color.red;
        Gizmos.DrawLine(groundCheck.position, groundCheck.position + Vector3.down * groundCheckDistance);
    }

    /// <summary>
    /// 反转角色朝向，根据水平输入调整角色的 localScale.x 来实现翻转效果。
    /// </summary>
    private void Flip()
    {
        if (Input.GetAxisRaw("Horizontal") > 0)
        {
            transform.localScale = new Vector3(1, 1, 1);
        }
        else if (Input.GetAxisRaw("Horizontal") < 0)
        {
            transform.localScale = new Vector3(-1, 1, 1);
        }
    }

    /// <summary>
    /// 玩家移动方法，直接设置 Rigidbody2D 的 velocity 来实现移动。
    /// 根据输入方向调整水平速度，同时保留垂直速度（如重力和跳跃）。
    /// 动画状态根据实际速度更新，确保动画与移动同步。
    /// </summary>
    /// <param name="dir">输入的方向</param>
    private void Move(float dir)
    {
        // 直接设置速度，保留 y 轴速度（用于重力、跳跃等）
        rb.velocity = new Vector2(dir * moveSpeed, rb.velocity.y);

        // 根据实际速度（而非输入）更新动画，避免输入与移动不同步
        bool isMoving = Mathf.Abs(rb.velocity.x) > 0.01f;
        anim.SetBool("isMove", isMoving);
    }

    /// <summary>
    /// 跳跃方法
    /// </summary>
    private void Jump()
    {
        if(Input.GetKeyDown(KeyCode.Space) && isGrounded)
        {
            rb.velocity = new Vector2(rb.velocity.x, jumpForce);
        }
    }

    /// <summary>
    /// 检测玩家是在下降还是上升状态，更新动画参数以反映跳跃状态。
    /// </summary>
    private void CheckJumpState()
    {
        if(rb.velocity.y > 0.01f)
        {
            anim.SetBool("isJump", true);
            anim.SetBool("isFall", false);
        }
        else if(rb.velocity.y < -0.01f)
        {
            anim.SetBool("isJump", false);
            anim.SetBool("isFall", true);
        }
        else
        {
            anim.SetBool("isJump", false);
            anim.SetBool("isFall", false);
        }

    }
}
```
我得了一种看到代码在同一个文件里特别多就会死的病，所以必须重新设计规划一下我们的代码，把`PlayerController`变成一个精简的类，其他的全部分离出去，好让后面的我们拓展代码。。。。。（我写不动了，交给明天的自己吧TvT）

留一个链接，是代码设计模式方面的，我感觉讲的非常好，最近一直在看：[游戏设计模式]

[游戏设计模式]: https://gpp.tkchu.me/introduction.html