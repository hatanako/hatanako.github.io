---
title: Unity复习学习随笔（一）：Unity基础
date: 2025-11-24
categories: 编程笔记
tags:
  - Unity
---

## 一、工程文件夹

在unity新建工程后会产生一个工程文件夹，在这个文件夹内部一共有6个文件夹，分别是：

> **1.Assets：工程资源文件夹（美术资源，脚本等等）**  
> **2.Library：库文件夹（Unity自动生成管理）**  
> 3.Logs：日志文件夹，记录特殊信息（Unity自动生成管理）  
> 4.obj：编译产生的中间文件（Unity自动生成管理）  
> 5.Packages：包配置信息（Unity自动生成管理）  
> **6.ProjectSettings：工程设置信息（Unity自动生成管理）**

主要使用的资源类型：

> 图片格式：jpg、png、tga  
> 模型格式：fbx、max、maya  
> 音效：wav、mp3、ogg  
> 文本：txt、json、bytes  
> 视频：mp4

各类层级窗口的作用：

> Inspector检查窗口：用于设置游戏对象具体信息  
> Console控制台窗口：用于显示调试信息，报错、警告、打印信息等等  
> Scene场景窗口：所有游戏对象所在地  
> Hierarchy层级窗口：所有游戏的对象名单  
> Game游戏窗口：玩家看到的游戏画面  
> Project工程窗口：所有游戏资源和脚本内容

## 二、Unity中的反射机制

Unity引擎本质是一个软件，使用它时是处于运行中的，我们是在一个运行中的软件里制作游戏的  
Unity开发的本质就是在Unity引擎的基础上，利用反射和引擎提供的各种功能进行的拓展开发

反射的概念：  
程序正在运行时，可以查看其他程序集或者自身的元数据  
一个运行的程序查看本身或者其他程序的元数据的行为就叫做反射

在程序运行时，通过反射可以得到其他程序集或者自己程序集中代码的各种信息  
比如类、函数、变量、对象等等，我们可以实例化它们，执行它们，操作它们

### 场景中对象的本质是什么

GameObject类对象是Unity引擎提供给我们的，作为场景中所有对象的根本，在游戏场景中出现一个对象，不管是图片、模型、音效、摄像机等等都是依附于GameObject对象的

### Transform是什么

GameObject对象作为一个出现在舞台（场景）中的成员，必须有一个表示自己所在位置的信息  
Transform就是一个必不可少的 脚本 ，它的本质就是发了一个位置给对象  
用于设置和得到演员在世界中的位置角度缩放等信息

### 反射机制的体现

除了Transform这个表示位置的脚本外，我们可以为GameObject关联各种C#脚本  
让它按照我们代码逻辑中的命令来处理事情，而为对象添加脚本的过程，就是在利用反射new一个新的脚本对象和GameObject对象进行关联，让其按照我们的命令做事

**例**：

前提：Unity帮助我们实现了对象查找和关联  
1.修改Inspector面板中Transform的内容  
利用反射：已知对象，类名，变量名，通过反射为该对象设置变量值  
2.新建一个脚本后，添加给一个指定的GameObject对象  
利用反射：已知类名，可以获取所有公共成员，故可以在Inspector面板上创建各公共字段信息

### 游戏场景的本质

游戏场景文件，后缀为.unity，它的本质就是一个配置文件  
Unity有一套自己识别处理它的机制，但是本质就是把场景对象相关信息读取出来  
通过反射来创建各个对象关联各个脚本对象

## 三、预制体和资源包的导入导出

**预制体是什么？**预制体就是预先设置好的物体

**如何创建预制体？**从场景中直接拖入Assets文件夹中

**如何修改预制体？**  
1.在场景中修改后，在Inspector组件中选择Override，然后点击Apply All（应用到所有）  
2.直接拖入预制体  
3.直接双击打开或者在Inspector选择Open后修改

**如何删除预制体？**  
我们可以通过右键选中场景中的预制体，然后点击Unpack Prefab，这样不会被预制体关联了

**资源包的导入导出**

{% asset_img 1.png %}

注意导出内容最好不要使用中文。

**如何导入？**可以使用Project窗口右键Import，也可以直接拖入

## 四、脚本的基本规则

创建规则：

> 1.不在VS中创建脚本  
> 2.可以放在Assets文件夹下的任意位置（建议同一文件夹管理）  
> 3.类名和文件名必须一致，不然不能挂载（因为反射机制创建对象，会通过文件名去找Type）  
> 4.最好不要使用中文命名  
> 5.没有特殊需求的话，不用管命名空间  
> 6.创建的脚本默认继承MonoBehavior

### 1.MonoBehaviour基类

> 1.创建的脚本默认都继承 _MonoBehaviour_ 继承了它才能够挂载在GameObject上  
> 2.继承了_MonoBehaviour_的脚本不能new 只能挂载  
> 3.继承了_MonoBehaviour_的脚本不要去写构造函数，因为我们不会去new它，写构造函数没有任何意义  
> 4.继承了_MonoBehaviour_的脚本可以在一个对象上挂多个  
> （如果没有加_DisallowMultipleComponent_特性）  
> 5.继承_MonoBehaviour_的类也可以再次被继承，遵循面向对象继承多态的规则

补充：_DisallowMultipleComponent_ 在 Unity 中，_DisallowMultipleComponent_ 是一个特性（Attribute），用于防止在同一个 GameObject 上多次添加相同类型（或子类型）的 MonoBehaviour 脚本。这个特性确保了在一个 GameObject 上只能存在一个同种类型的脚本，避免了可能由于手误而添加多个相同脚本的问题。

### 2.不继承MonoBehaviour的类

1.不继承Mono的类**不能挂载**在GameObject上  
2.不继承Mono的类想怎么写就怎么写，**<u>如果要使用需要自己new</u>**  
3.不继承Mono的类一般是**单例模式的类（用于管理模块）或者数据结构类（用于存储数据）**  
4.不继承Mono的类不用保留默认出现的几个函数

### 3.执行的先后顺序

我们目前是没有办法去 控制 脚本的执行顺序的，但是Unity给我们提供了一个Script Execution Order，通过这个窗口，我们可以来控制脚本执行顺序

{% asset_img 2.png %}

{% asset_img 3.png %}

### 4.默认脚本内容

可以通过（Unity版本目录）Editor\\Data\\Resources\\ScriptTemplates中修改

**练习**：

{% asset_img 4.png %}

答：  
1.因为MonoBehaviour不能new 只能挂载  
我们不会通过 构造函数 实例化对象  
写构造函数没有意义

2.这是Unity定下的规则，继承MonoBehaviour的类只能挂载在GameObject上配合使用

## 五、生命周期函数

### 1.帧的概念

游戏的本质就是一个死循环，每一次循环处理游戏逻辑就会更新一次画面  
之所以能看到画面在动是因为切换画面的速度达到一定时，人眼就会认为画面是流畅的  
一帧就是一次循环

fps（Frames Per Second）：  
即每秒钟帧数，一般我们说的60帧30帧，意思就是1秒更新60次、30次画面  
1s = 1000ms  
60帧：1帧为 1000ms/60 ≈ 16.66ms  
30帧：1帧为 1000ms/30 ≈ 33.33ms  
其他同理  
人眼舒适放松时可视帧数是每秒24帧

游戏卡顿的原因：  
跑1帧游戏逻辑中的计算量过大，或者CPU不给力，不能在一帧的时间内处理完所有游戏逻辑

Unity 的底层已经帮助我们做好了死循环，我们需要学习Unity的生命周期函数，利用它做好的规则来执行我们的游戏逻辑就行了。

### 2.生命周期函数的概念

所有继承MonoBehaviour的脚本，最终都会挂载到GameObject游戏对象上  
**生命周期函数**就是该脚本依附的GameObject对象从出生到消亡的整个生命周期中  
会通过反射自动调用的一些特殊函数

Unity帮助我们记录了一个GameObject对象衣服了哪些脚本，会自动的得到这些对象  
通过反射去执行一些固定名字的函数

### 3.生命周期函数

注意：生命周期函数的访问修饰符一般为private和protected  
因为不需要在外部自己调用生命周期函数，都是Unity自己帮助我们调用的  
所有生命周期函数都需要继承MonoBehaviour使用

#### 补充：在Unity中打印信息的两种方式

1.没有继承Mono类的时候：

```cs
Debug.Log("123");
Debug.LogError("出错了");
Debug.LogWarning("警告");
```

2.继承了Mono类：

```cs
print("123123");
```

#### Awake

当对象（自己这个类对象）被创建时才会调用该生命周期函数，类似构造函数的存在  
我们可以在一个类对象被创建时进行一些初始化操作

#### OnEnable

依附的GameObject对象每次激活时调用  
作用：可以当一个对象被激活时，进行一些逻辑处理

#### Start

从自己被创建出来后，第一次帧更新之前调用，一个对象只会调用一次  
作用：主要作用还是用于初始化信息的，但是它相对Awake来说要晚一点  
因为它是在对象，进行第一次帧更新之前才会执行的

#### FixedUpdate

物理帧更新，固定间隔时间执行，间隔时间可以设置  
作用：它主要是用于进行物理更新，它是每一帧都执行的，但是这里的帧和游戏帧有点不同

如何更改FixedUpdate的帧速率？

{% asset_img 5.png %}

通过更改Fixed Timestep来改变帧更新速率

#### Update

逻辑帧更新，每帧执行  
作用：主要用于处理游戏核心逻辑更新的函数

#### LateUpdate

每帧执行，于Update之后执行  
作用：一般这个更新是用来处理摄像机位置更新相关内容的  
Update和LateUpdate之间，Unity进行了一些处理，来处理我们动画相关的更新

#### OnDisable

依附的GameObject对象每次失活时调用  
作用：如果我们希望在一个对象失活时做一些处理，就可以在该函数中写逻辑

#### OnDestroy

依附的GameObject对象被删除时，对象销毁时调用

**注：生命周期函数支持继承多态**

**为什么Unity可以自动执行这些函数？**

**Unity帮助我们记录了场景上的所有GameObject对象以及各个关联的脚本对象，在游戏执行的特定时机，它会通过函数名反射得到脚本对象中对应的生命周期函数，然后在这些特定时机执行他们。**

**不同对象的生命周期函数是在同一个线程中执行的吗？**

答：Unity会主动把场景上的对象，对象上挂载的脚本都统统记录下来，在主线程的死循环中，按顺序按时机的通过反射，执行记录的对象身上挂载的脚本的对应生命周期函数。

## **六、Inspector窗口可编辑的变量**

Inspector显示的可编辑内容就是脚本的成员变量

**1.私有和保护变量是无法显示编辑的**

那么如何让私有和保护变量被编辑呢？

我们可以给它们加上强制序列化字段特性：\[SerializeField\]  
所谓序列化就是把一个对象保存到一个文件或者数据库字段中去

**2.公共变量可以显示编辑**

如何让公共变量不被编辑？

我们可以给它们加上特性：\[HideInInspector\]

**3.大部分类型都能显示编辑**

字典和自定义类型不能被Inspector窗口显示

**4.如何让自定义类型也能被Inspector窗口显示**

加上序列化特性\[System.Serializable\]  
注：字典怎么样都不行

**5.一些辅助特性**

1.分组说明特性： Header  
为成员分组：\[Header("分组说明")\]

2.悬停注释特性：Tooltip  
为变量添加说明：\[Tooltip("说明内容")\]

3.间隔特性：Space()  
让两个字段间出现间隔：\[Space()\]

4.修饰数值的滑条范围Range  
\[Range(最小值，最大值)\]

5.多行显示字符串，默认不写参数显示3行，写参数就是对应行  
\[Multiline(行数)\]

6.滚动条显示字符串（默认不写参数就是超过3行显示滚动条）  
\[TextArea(最少行数，最多行数)\] 最少显示多少行，最多多少行，超过最多行就显示滚动条

7.为变量添加快捷方法：ContextMenuItem  
参数1：显示按钮名  
参数2：方法名，不能有参数  
\[ContextMenuItem(“显示按钮名”,"方法名")\]

8.为方法添加特性能够在Inspector中执行：ContextMenu  
\[ContextMenu("测试函数")\]

**6.注意**

1.Inspector窗口中的变量关联的就是对象的成员变量，运行时改变他们就是在改变成员变量  
2.拖拽到GameObject对象后，再改变脚本中的变量默认值，界面上不会改变  
3.运行中修改的信息不会保存

**练习**：

{% asset_img 6.png %}

## **七、Mono中的重要内容**

### **1.重要成员**

**1.获取依附的gameObject**

通过 _this.gameObject_ 可以直接获得挂载此脚本的GameObject，可以通过 _.name_ 来获取GameObject的名字

**2.获取依附的GameObject的位置信息**

通过 _this.transform.posion_ 来得到对象的位置信息  
通过 _this.transform.eulerAngles_ 来得到对象的角度信息  
通过 _this.transform.lossyScale_ 来得到对象的缩放大小信息

也可以使用 _this.gameObject.transform_ 这种写法和上面效果相同，都可以得到依附的对象的位置信息

**3.获取脚本是否激活**

通过 _this.enabled_ 来控制脚本激活

### **2.重要方法**

得到依附对象上挂载的其他脚本  
**1.得到自己挂载的单个脚本**  
根据脚本名获取：

```cs
Test t = this.GetComponent("Test") as Test;
```

根据Type获取：

```cs
t = this.GetComponent(typeof(Test)) as Test;
```

根据泛型获取，建议使用泛型获取，因为不用二次转换

```cs
t = this.GetComponent<Test>();
```

只要你能得到场景中别的对象或者对象依附的脚本，那么你就可以获取到它的所有信息

**2.得到自己挂载的多个脚本**

```cs
Test[] t = this.GetComponents<Test>();
List<Test> list = new List<Test>();
this.GetComponents<Test>(list);
```

**3.得到子对象挂载的脚本（它默认会找自己身上是由挂载了该脚本）**

```cs
t = this.GetComponentInChildren<Test_Child>();
```

函数是有一个参数的，默认不传是false，意思就是如果子对象失活，是不会去找到这个对象上是否有某个脚本的  
如果传true，即使失活也会查找

得到子对象同样有复数，可以得到多个脚本 _GetComponentsInChildren_

**4.得到父对象挂载的脚本（同上）**此函数没有参数

**5.尝试获取脚本**

```cs
Test t1 = null;
if(this.TryGetComponent<Test>(out t1))
{
}
```

提供了一个更加安全的获取单个脚本的方法，如果得到了会返回true，然后来进行逻辑处理即可

**练习**：

{% asset_img 7.png %}